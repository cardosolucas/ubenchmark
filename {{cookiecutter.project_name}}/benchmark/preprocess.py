import pandas as pd

mocked_data = {
        'train_y': pd.DataFrame(),
        'train_x': pd.DataFrame(),
        'test_x': pd.DataFrame(),
        'test_y': pd.DataFrame()
    }

def preprocess():
    return mocked_data